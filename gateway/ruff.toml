# Ruff linter configuration for StackDeploy Dashboard Gateway
# See: https://docs.astral.sh/ruff/

target-version = "py311"

[lint]
select = ["F", "E", "W", "I"]
ignore = [
  "E501",  # line-too-long — keep relaxed for readability
]
